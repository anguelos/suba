# =============================================================================
# suba — Single-cell Universal Base Addressing
# requirements.txt  (pinned to versions used during development)
# =============================================================================
# Install everything:
#   pip install -r requirements.txt
#
# Install only the runtime core:
#   pip install .
#
# Install with CLI extras:
#   pip install ".[cli]"
#
# Install with dev extras:
#   pip install ".[dev]"
# =============================================================================

# ---------------------------------------------------------------------------
# Core (required at runtime)
# ---------------------------------------------------------------------------
numpy>=1.24,<3          # all array operations; universal-coordinate dtype int64
requests>=2.28          # resumable HTTP download of GTF / chrom.sizes
tqdm>=4.64              # download progress bar
scipy>=1.10             # sparse CSR matrices for CountMatrixTracks

# ---------------------------------------------------------------------------
# CLI extras  (pip install ".[cli]")
# ---------------------------------------------------------------------------
fargv>=1.3              # dataclass-based CLI argument parsing
matplotlib>=3.7         # figure saving / display
seaborn>=0.12           # Hilbert-curve heatmap (used as thin wrapper over imshow)

# ---------------------------------------------------------------------------
# Single-cell I/O extras
# ---------------------------------------------------------------------------
h5py>=3.8               # .h5ad / HDF5 count-matrix I/O (load_count_matrix_h5)
anndata>=0.9            # AnnData round-trip (optional, for interop with scanpy)
scanpy>=1.9             # single-cell analysis ecosystem (optional interop)

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0         # --cov flag; produces .coverage + XML + HTML reports
coverage[toml]>=7.0     # coverage.py with pyproject.toml config support

# ---------------------------------------------------------------------------
# Coverage badge
# ---------------------------------------------------------------------------
genbadge[coverage]>=1.1 # generate static SVG badge from coverage XML
                        # (alternative to coverage-badge, no pkg_resources dep)

# ---------------------------------------------------------------------------
# Documentation
# ---------------------------------------------------------------------------
sphinx>=7.0             # HTML doc builder
myst-parser>=3.0        # Markdown (.md) source files in Sphinx
sphinx-autoapi>=3.0     # API docs auto-generated from docstrings

# ---------------------------------------------------------------------------
# Build
# ---------------------------------------------------------------------------
setuptools>=68          # PEP 517 build backend (pyproject.toml)
wheel>=0.41             # bdist_wheel
